% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate_ews.R, R/summary.R
\name{detect_multivariate_warnings}
\alias{detect_multivariate_warnings}
\alias{summary.multi_ews}
\title{Detect Multivariate Early Warning Signals}
\usage{
detect_multivariate_warnings(
  data,
  time_col = NULL,
  metrics = "all",
  method = "rolling",
  window = 50,
  burn_in = 10L,
  threshold = 2,
  tail_direction = "one.tailed"
)

\method{summary}{multi_ews}(object, ...)
}
\arguments{
\item{data}{[\code{data.frame}]\cr
A data frame with a time column and at least two numeric time series
columns. If \code{time_col} is not specified, the first column is assumed
to be time.}

\item{time_col}{[\code{character(1)} or \code{NULL}: \code{NULL}]\cr
Name of the time column. If \code{NULL}, the first column is used.}

\item{metrics}{[\code{character()}: \code{"all"}]\cr
Metrics to compute. Pass \code{"all"} for all eleven, or a subset of:
\code{"meanSD"}, \code{"maxSD"}, \code{"meanAR"}, \code{"maxAR"}, \code{"eigenMAF"},
\code{"mafAR"}, \code{"mafSD"}, \code{"pcaAR"}, \code{"pcaSD"}, \code{"eigenCOV"},
\code{"maxCOV"}.}

\item{method}{[\code{character(1)}: \code{"rolling"}]\cr
Analysis approach: \code{"rolling"} or \code{"expanding"}.}

\item{window}{[\code{numeric(1)}: \code{50}]\cr
For \code{"rolling"}: window size as a percentage of total series length.
For \code{"expanding"}: not used directly (the window grows from
\code{min_window} to \code{n}).}

\item{burn_in}{[\code{integer(1)}: \code{10L}]\cr
Number of initial observations after which warnings begin to be
flagged. Only used when \code{method = "expanding"}.}

\item{threshold}{[\code{numeric(1)}: \code{2}]\cr
Number of standard deviations for warning detection. Only used when
\code{method = "expanding"}.}

\item{tail_direction}{[\code{character(1)}: \code{"one.tailed"}]\cr
Whether to test for warnings using \code{"one.tailed"} (positive
deviations only) or \code{"two.tailed"} (both directions). Only used when
\code{method = "expanding"}.}

\item{object}{[\code{multi_ews}]\cr
A \code{multi_ews} object.}

\item{...}{Additional arguments (currently unused).}
}
\value{
An object of class \code{"multi_ews"}, a tibble (inheriting from
\code{tbl_df}, \code{tbl}, \code{data.frame}) with metadata stored as attributes.

\strong{Rolling} columns: \code{time}, \code{metric}, \code{score}, \code{std}.
Attributes: \code{orig_data}, \code{cor} (Kendall tau per metric),
\code{method} (\code{"rolling"}), \code{dimension_reduction}.

\strong{Expanding} columns: \code{time}, \code{metric}, \code{score}, \code{z_score},
\code{detected} (integer 0/1).
Attributes: \code{orig_data}, \code{threshold}, \code{tail_direction},
\code{classification} (tibble with \code{time}, \code{count}, \code{state}),
\code{method} (\code{"expanding"}), \code{dimension_reduction}.

A \code{summary.multi_ews} object. Identical to \code{object}.
}
\description{
Performs multivariate early warning signal (EWS) analysis on a system
represented by multiple time series. The function computes a suite of
metrics --- based on Min/Max Autocorrelation Factors (MAF), Principal
Component Analysis (PCA), and covariance structure --- over rolling or
expanding windows to detect rising instability before critical transitions.
}
\details{
Multivariate EWS extend univariate indicators to systems where several
variables interact. Individual-variable summaries (mean/max of standard
deviations and AR(1) coefficients) are complemented by dimension-reduction
indicators that capture coordinated changes across variables.

The eleven available metrics are:
\itemize{
\item \code{meanSD}: Mean standard deviation across all variables in the
window. Rising values indicate broadening fluctuations system-wide.
\item \code{maxSD}: Maximum standard deviation among variables. Flags when any
single variable becomes highly volatile.
\item \code{meanAR}: Mean lag-1 autoregressive coefficient across variables.
Increasing values signal system-wide critical slowing down.
\item \code{maxAR}: Maximum AR(1) coefficient among variables. Detects
critical slowing down concentrated in a single variable.
\item \code{eigenMAF}: Ratio of the minimum eigenvalue of the MAF
decomposition to the total. Declining ratios (increasingly negative
after sign inversion) indicate rising spatial correlation
(Dakos et al., 2012).
\item \code{mafAR}: AR(1) coefficient of the first MAF component. The MAF
transformation orders components by temporal smoothness, so the first
component captures the slowest mode. Rising mafAR indicates critical
slowing down in the most persistent system dimension.
\item \code{mafSD}: Standard deviation of the first MAF component. Sign-inverted
so that rising values indicate increasing fluctuation in the slowest
mode.
\item \code{pcaAR}: AR(1) coefficient of the first principal component. PCA
captures the dominant variance direction; rising pcaAR signals that
this direction is becoming more persistent.
\item \code{pcaSD}: Standard deviation of the first principal component.
Rising values indicate that the dominant variance mode is
amplifying.
\item \code{eigenCOV}: Dominant eigenvalue of the covariance matrix of
standardized data. Increasing values indicate growing variance
concentration along one axis.
\item \code{maxCOV}: Maximum off-diagonal element of the covariance matrix.
Rising values signal strengthening cross-variable coupling.
}

\strong{Rolling window} (\code{method = "rolling"}): a fixed-width window slides
across the series. Each metric is standardized (z-scored) across all
window positions for comparability. Kendall's tau trend statistic
is computed for each metric against time; metrics with
\eqn{\tau > 0.7} are flagged as showing significant upward trends.

\strong{Expanding window} (\code{method = "expanding"}): the window starts at
\code{min_window} and grows one observation at a time. A two-stage
standardization is applied (matching the EWSmethods implementation):
(1) each metric's raw value is z-scored against its own expanding
history up to that point; (2) the resulting z-scores are z-scored
again using an expanding cumulative mean and SD. This double
standardization puts all metrics on a common dimensionless scale
regardless of their original units. A warning is flagged when the
doubly-standardized strength exceeds \code{threshold} after the
\code{burn_in} period. Classification into system states (Stable,
Vulnerable, Warning, Critical, Failing) is based on
how many metrics simultaneously cross the threshold.

\strong{Dimension reduction series}: regardless of method, the function
also extracts the first MAF (MAF1) and first principal component
(PC1) time series, standardizes them, and flags points where the
absolute scaled value exceeds the threshold. These provide a
low-dimensional summary of the system's trajectory.
}
\section{Functions}{
\itemize{
\item \code{summary(multi_ews)}: Summary method for \code{"multi_ews"}
objects. Provides a condensed overview including per-metric warning
counts and system state distribution.

}}
\examples{
\donttest{
tip <- generate_tipping_data(n_time = 100, n_vars = 3, tipping_point = 60)
ews <- detect_multivariate_warnings(tip, method = "expanding", window = 50)
plot(ews)

ews_roll <- detect_multivariate_warnings(tip, method = "rolling", window = 50)
plot(ews_roll)
}
}
\references{
Dakos, V., Carpenter, S. R., Brock, W. A., Ellison, A. M., Guttal, V.,
Ives, A. R., ... & Scheffer, M. (2012). Methods for detecting early
warnings of critical transitions in time series illustrated using
simulated ecological data. \emph{PLoS ONE}, 7(7), e41010.
\doi{10.1371/journal.pone.0041010}

Scheffer, M., Bascompte, J., Brock, W. A., Brovkin, V., Carpenter,
S. R., Dakos, V., ... & Sugihara, G. (2009). Early-warning signals
for critical transitions. \emph{Nature}, 461(7260), 53--59.
\doi{10.1038/nature08227}
}
\seealso{
\code{\link[=generate_tipping_data]{generate_tipping_data()}} for creating test data with known
tipping points; \code{\link[=plot.multi_ews]{plot.multi_ews()}} for visualization.
}
