% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.trend}
\alias{plot.trend}
\title{Plot Trend Classification Results}
\usage{
\method{plot}{trend}(x, type = "series", ...)
}
\arguments{
\item{x}{[\code{trend}]\cr
An object of class \code{trend} as returned by \code{\link[=compute_trend]{compute_trend()}}.}

\item{type}{[\code{character(1)}: \code{"series"}]\cr
Plot type: \code{"series"} for background shading, \code{"ribbons"} for a
ribbon bar beneath the time series.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Visualizes the time series with trend state classification, either as
background-shaded regions or as a ribbon bar beneath the series.
}
\details{
Two plot types are available:

\strong{\code{type = "series"} (default).} The time series is drawn with colored
background tiles indicating the trend state at each time point. This
provides an immediate overlay of direction and trajectory.

\strong{\code{type = "ribbons"}.} The time series is drawn in the upper portion,
with a coloured ribbon bar below showing the trend state sequence. This
matches the layout used by \code{\link[=plot.resilience]{plot.resilience()}} and makes it easy to
compare trend and resilience classifications side by side.
}
\examples{
\donttest{
set.seed(42)
x <- cumsum(rnorm(200, sd = 2))
tr <- compute_trend(x, window = 20)
plot(tr)
plot(tr, type = "ribbons")
}
}
\seealso{
\code{\link[=compute_trend]{compute_trend()}} for computing trend classifications;
\code{\link[=plot.resilience]{plot.resilience()}} for the analogous resilience ribbon plot.
}
\concept{time series}
\concept{trend}
