% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datagen.R
\name{generate_ts_data}
\alias{generate_ts_data}
\title{Generate Synthetic Time Series Data}
\usage{
generate_ts_data(
  n_individuals = 1L,
  data_length = 500L,
  data_type = "resilience",
  n_stable_levels = 3L,
  mean_stable_value = 100,
  level_separation = 20,
  n_levels = 3L,
  results_df = NULL,
  results_col = NULL,
  seed = 42L,
  generate_plot = TRUE
)
}
\arguments{
\item{n_individuals}{[\code{integer(1)}: \code{1}]\cr
Number of distinct individuals to generate.}

\item{data_length}{[\code{integer(1)}: \code{500}]\cr
Number of data points per individual.}

\item{data_type}{[\code{character(1)}: \code{"resilience"}]\cr
One of \code{"resilience"} or \code{"clustered"}.}

\item{n_stable_levels}{[\code{integer(1)}: \code{3}]\cr
Number of stable levels (resilience mode).}

\item{mean_stable_value}{[\code{numeric(1)}: \code{100}]\cr
Central value of the stable phase.}

\item{level_separation}{[\code{numeric(1)}: \code{20}]\cr
Percentage separation between stable levels.}

\item{n_levels}{[\code{integer(1)}: \code{3}]\cr
Number of clusters (clustered mode, 2--6).}

\item{results_df}{[\code{data.frame} or \code{NULL}]\cr
Optional external results for comparison plotting.}

\item{results_col}{[\code{character(1)} or \code{NULL}]\cr
Column in \code{results_df} with classification labels.}

\item{seed}{[\code{integer(1)}: \code{42}]\cr
Random seed for reproducibility.}

\item{generate_plot}{[\code{logical(1)}: \code{TRUE}]\cr
If \code{TRUE}, return a list with \verb{$data} and \verb{$plot}.}
}
\value{
Depends on \code{generate_plot}:
\itemize{
\item If \code{generate_plot = TRUE}, a \code{list} with:
\describe{
\item{data}{A \code{data.frame} with columns \code{time}, \code{value},
\code{true_phase}, and optionally \code{id} (when \code{n_individuals > 1}).
Clustered mode also includes \code{quantile_class}.}
\item{plot}{A \code{ggplot} or \code{patchwork} object showing the time
series with classification bands.}
}
\item If \code{generate_plot = FALSE}, the \code{data.frame} directly.
}
}
\description{
Creates synthetic time series with known ground-truth phases for
testing resilience, classification, and early-warning analyses.
Supports two modes: \code{"resilience"} (dynamic phases with shocks,
recovery, volatility) and \code{"clustered"} (distinct value levels).
}
\details{
Two generation modes are available:

\strong{Resilience mode} (\code{data_type = "resilience"}): produces a time series
composed of randomly ordered dynamic phases, with approximately 50\\% of
the total length allocated to stable segments. The phases are:
\describe{
\item{Stable}{An AR(1) process with a low autoregressive coefficient
(0.2) and small noise (sd = 0.5), centered on a given baseline value.
When \code{n_stable_levels > 1}, multiple stable segments are created at
distinct operating baselines separated by \code{level_separation} percent
of \code{mean_stable_value}.}
\item{Shock + Recovery}{A sharp linear drop to 70\\% below baseline
(the Shock sub-phase), followed by a linear climb back to baseline
(the Recovery sub-phase). Both include small Gaussian perturbations.}
\item{Volatile}{An AR(1) process with the same autoregressive
coefficient as Stable but much larger noise (sd = 15), producing
high-amplitude fluctuations around the baseline.}
\item{Turbulent}{A random walk (cumulative sum of Gaussian increments,
sd = 8), creating drift without a restoring force.}
}
The phases are placed in a random order so that the generated series is
suitable for testing blind classification algorithms.

\strong{Clustered mode} (\code{data_type = "clustered"}): creates contiguous
segments at 2--6 distinct value levels, each with controlled
within-cluster Gaussian noise (sd = 1). The function also appends a
\code{quantile_class} column containing a naive quantile-based classification
for benchmarking against the true labels.
}
\examples{
# Resilience mode --returns data.frame only
set.seed(1)
df <- generate_ts_data(
  n_individuals = 1, data_length = 200,
  data_type = "resilience", generate_plot = FALSE
)
head(df)
table(df$true_phase)

# Clustered mode --returns data.frame only
df_cl <- generate_ts_data(
  data_length = 300, data_type = "clustered",
  n_levels = 4, generate_plot = FALSE
)
head(df_cl)

\donttest{
# Resilience mode with plot
out <- generate_ts_data(
  n_individuals = 2, data_length = 500,
  data_type = "resilience", generate_plot = TRUE
)
out$plot
}
}
\seealso{
\code{\link[=compare_ts]{compare_ts()}} for visualising classification results,
\code{\link[=resilience]{resilience()}} for computing resilience metrics,
\code{\link[=hurst]{hurst()}} for long-range dependence analysis.

Other data generation: 
\code{\link{compare_ts}()},
\code{\link{generate_tipping_data}()}
}
\concept{data generation}
\concept{synthetic data}
\concept{time series}
