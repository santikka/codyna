% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resilience.R
\name{classify_resilience}
\alias{classify_resilience}
\title{Classify Resilience Scores}
\usage{
classify_resilience(
  data,
  method = "empirical_state_aware",
  weights = NULL,
  smooth_window = 10L,
  smooth_method = "median"
)
}
\arguments{
\item{data}{[\code{resilience}]\cr
An object of class \code{resilience} as returned by \code{\link[=resilience]{resilience()}}.}

\item{method}{[\code{character(1)}: \code{"empirical_state_aware"}]\cr
Normalization method for scaling raw metric values. The available
options are:
\itemize{
\item \code{"empirical_state_aware"}: State-aware scaling using empirical
thresholds derived from known resilience phases.
\item \code{"empirical_percentile"}: Hardcoded percentile-bin scaling.
\item \code{"percentile"}: Empirical CDF (rank-based) scaling.
\item \code{"minmax"}: Min-max scaling to [0, 1].
\item \code{"zscore"}: Z-score standardization.
\item \code{"divide_max"}: Division by absolute maximum.
}}

\item{weights}{[\code{numeric()}: \code{NULL}]\cr
Named numeric vector of metric weights for the composite score.
If \code{NULL}, empirically derived default weights are used.}

\item{smooth_window}{[\code{integer(1)}: \code{10L}]\cr
Window size for post-scaling median smoothing (applied per-metric).
Set to \code{1L} to disable smoothing.}

\item{smooth_method}{[\code{character(1)}: \code{"median"}]\cr
Smoothing method: \code{"median"} or \code{"mean"}.}
}
\value{
The input \code{"resilience"} object augmented with additional columns:
\itemize{
\item \verb{\{metric\}_score} -- directionally normalized score in [0, 1]
for each computed metric (e.g., \code{vsi_score}, \code{cv_score}).
\item \verb{\{metric\}_category} -- ordered factor label
(\code{"Excellent"} through \code{"Troubled"}) for each metric.
\item \code{composite_score} -- weighted average of all per-metric scores.
\item \code{composite_category} -- ordered factor label for the composite
score.
}

All original columns (\code{time}, \code{value}, and the raw metric columns) are
preserved. The returned object retains the \code{"resilience"} class and
its attributes.
}
\description{
Applies directional normalization to resilience metrics and computes
a weighted composite score with category labels.
}
\details{
Raw metric values from \code{\link[=resilience]{resilience()}} are not directly comparable because
they live on different scales and some are "lower is better" while others
are "higher is better." This function resolves both problems in three
steps:

\strong{1. Directional normalization.} Each metric is rescaled to a [0, 1]
score where 0 means "Excellent resilience" and 1 means "Troubled
resilience," regardless of the original direction. Metrics where lower raw
values indicate better resilience (vsi, arch_lm, cv, recovery_time,
dfa_alpha) are scaled so that low raw values map to scores near 0.
Metrics where higher raw values indicate better resilience
(recovery_slope, sample_entropy, ac_ratio) are inverted accordingly.

The \code{"empirical_state_aware"} method (the default) derives scaling
thresholds from empirically observed means for known resilience phases
(Stable, Recovery, Shock, Turbulent, Volatile). "Excellent" and
"Troubled" anchor points are set from quantiles of the good-state and
bad-state phase means, respectively. Alternative methods
(\code{"empirical_percentile"}, \code{"percentile"}, \code{"minmax"}, \code{"zscore"},
\code{"divide_max"}) use purely data-driven scaling.

\strong{2. Smoothing.} After scaling, each metric's score trajectory is
smoothed with a rolling median (or mean) of width \code{smooth_window} to
reduce transient noise in the classification. Set \code{smooth_window = 1L}
to disable smoothing.

\strong{3. Weighted composite.} The per-metric scores are combined into a
single \code{composite_score} using empirically derived weights that reflect
each metric's importance for distinguishing resilience phases.
The default weights are: vsi = 0.256, cv = 0.226, arch_lm = 0.188,
ac_ratio = 0.070, recovery_slope = 0.058, dfa_alpha = 0.036,
sample_entropy = 0.028, recovery_time = 0.016.

Both per-metric and composite scores are binned into six ordered
categories:
\itemize{
\item \strong{Excellent} (0.00--0.15)
\item \strong{Solid} (0.15--0.30)
\item \strong{Fair} (0.30--0.50)
\item \strong{Vulnerable} (0.50--0.70)
\item \strong{Failing} (0.70--0.85)
\item \strong{Troubled} (0.85--1.00)
}
}
\examples{
\donttest{
set.seed(42)
x <- cumsum(rnorm(500))
res <- resilience(x, window = 50L)
cls <- classify_resilience(res)
cls

# Ribbon plot with classified scores
plot(cls)
}
}
\seealso{
\code{\link[=resilience]{resilience()}} for computing the raw metrics;
\code{\link[=plot.resilience]{plot.resilience()}} for visualization of classified results.

Other resilience: 
\code{\link{plot.resilience}()},
\code{\link{resilience}()}
}
\concept{resilience}
\concept{time series}
