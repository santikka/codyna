% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.sensitivity_ews}
\alias{plot.sensitivity_ews}
\title{Plot Sensitivity Analysis Results}
\usage{
\method{plot}{sensitivity_ews}(x, type = "heatmap", ...)
}
\arguments{
\item{x}{[\code{sensitivity_ews}]\cr
An object of class \code{sensitivity_ews} as returned by
\code{\link[=sensitivity_ews]{sensitivity_ews()}}.}

\item{type}{[\code{character(1)}: \code{"heatmap"}]\cr
Plot type. The available options are:
\itemize{
\item \code{"heatmap"}: Tile plot of Kendall tau across parameter
combinations.
\item \code{"lines"}: Faceted line plots of metric trajectories.
\item \code{"both"}: Both panels stacked vertically.
}}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object (or a
\code{\link[patchwork:wrap_plots]{patchwork::wrap_plots()}} composite when \code{type = "both"}).
}
\description{
Visualizes how the Kendall tau trend statistic and metric trajectories
vary across parameter combinations (window size and detrending method).
}
\details{
Three plot types are available:

\strong{\code{type = "heatmap"}.} Displays a tile plot with window size on the
y-axis and detrend method on the x-axis. Each tile is colored by the
Kendall tau value using a diverging blue-white-red scale (blue =
negative tau, white = zero, red = positive tau). Consistently red
tiles across all combinations indicate a robust upward trend in the
EWS metric, providing confidence that the signal is not an artifact
of a specific parameter choice.

\strong{\code{type = "lines"}.} Faceted line plots showing the metric trajectory
over time for each (window, detrend) combination. Panels are faceted
by window size and detrend method, with free y-axes. This view reveals
how the raw metric evolution differs across parameter settings and
helps identify whether apparent trends are driven by specific window
sizes or detrending choices.

\strong{\code{type = "both"}.} Stacks the heatmap on top and the line plots
below using \code{\link[patchwork:wrap_plots]{patchwork::wrap_plots()}}, providing a complete overview
in a single figure.
}
\examples{
\donttest{
set.seed(42)
x <- cumsum(rnorm(300, sd = seq(0.5, 2, length.out = 300)))
sa <- sensitivity_ews(x, metric = "ar1")
plot(sa, type = "heatmap")
plot(sa, type = "lines")
plot(sa, type = "both")
}
}
\seealso{
\code{\link[=sensitivity_ews]{sensitivity_ews()}} for computing the sensitivity analysis.
}
