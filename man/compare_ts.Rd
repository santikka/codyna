% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datagen.R
\name{compare_ts}
\alias{compare_ts}
\title{Compare Time Series Classifications}
\usage{
compare_ts(
  data,
  data2 = NULL,
  ts_col,
  time_col = NULL,
  state1,
  state2 = NULL,
  facet_by = NULL,
  title1 = "Classification 1",
  title2 = "Classification 2",
  palette1 = "Set2",
  palette2 = "Set3"
)
}
\arguments{
\item{data}{[\code{data.frame}]\cr
Primary data containing the time series and \code{state1}.}

\item{data2}{[\code{data.frame} or \code{NULL}]\cr
Optional second data frame containing \code{state2}.}

\item{ts_col}{[\code{character(1)}]\cr
Column name for the time series values.}

\item{time_col}{[\code{character(1)} or \code{NULL}]\cr
Column name for the time axis. If \code{NULL}, row index is used.}

\item{state1}{[\code{character(1)}]\cr
Column name for the first classification.}

\item{state2}{[\code{character(1)} or \code{NULL}]\cr
Column name for the second classification.}

\item{facet_by}{[\code{character(1)} or \code{NULL}]\cr
Column name to facet by (e.g., individual ID).}

\item{title1}{[\code{character(1)}: \code{"Classification 1"}]\cr
Title for the first panel.}

\item{title2}{[\code{character(1)}: \code{"Classification 2"}]\cr
Title for the second panel.}

\item{palette1}{[\code{character(1)}: \code{"Set2"}]\cr
RColorBrewer palette for the first panel.}

\item{palette2}{[\code{character(1)}: \code{"Set3"}]\cr
RColorBrewer palette for the second panel.}
}
\value{
\itemize{
\item When \code{state2} is \code{NULL}: a single \code{ggplot} object.
\item When \code{state2} is provided: a \code{patchwork} object containing two
vertically stacked \code{ggplot} panels.
}
}
\description{
Visualizes one or two classification results as colored background
bands behind a time series line plot. Useful for comparing ground-truth
labels to model predictions.
}
\details{
Each classification is rendered as a panel containing the time series
drawn as a black line with classification labels shown as colored
background rectangles spanning the full y-axis range. Colors are
drawn from \link[RColorBrewer:ColorBrewer]{RColorBrewer} palettes
(\code{palette1} and \code{palette2}).

When only \code{state1} is provided the function returns a single \code{ggplot}
panel. When \code{state2} is also supplied (either from a column already in
\code{data} or from a separate \code{data2} data frame), two panels are
produced and stacked vertically using
\code{\link[patchwork:wrap_plots]{patchwork::wrap_plots()}}, making it straightforward to compare, for
example, ground-truth phases against a model's predicted labels.

If \code{facet_by} is set (e.g., to an individual-ID column), each panel
is additionally faceted with free y-scales so that multiple time
series can be displayed side by side.
}
\examples{
\donttest{
# Generate data and compare ground truth to a naive quantile classifier
df <- generate_ts_data(
  data_length = 300, data_type = "clustered",
  n_levels = 3, generate_plot = FALSE
)
# Single-panel plot
p1 <- compare_ts(
  data = df, ts_col = "value", time_col = "time",
  state1 = "true_phase", title1 = "True Clusters"
)
print(p1)

# Two-panel comparison
p2 <- compare_ts(
  data = df, ts_col = "value", time_col = "time",
  state1 = "true_phase", state2 = "quantile_class",
  title1 = "True Clusters",
  title2 = "Quantile Classification"
)
print(p2)
}
}
\seealso{
\code{\link[=generate_ts_data]{generate_ts_data()}} for creating synthetic time series with
known ground-truth phases suitable as input to this function.
}
