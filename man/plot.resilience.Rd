% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resilience.R
\name{plot.resilience}
\alias{plot.resilience}
\title{Plot Resilience Results}
\usage{
\method{plot}{resilience}(x, type = "ribbons", ...)
}
\arguments{
\item{x}{[\code{resilience}]\cr
An object of class \code{resilience}, optionally augmented by
\code{\link[=classify_resilience]{classify_resilience()}} (required for \code{type = "ribbons"}).}

\item{type}{[\code{character(1)}: \code{"ribbons"}]\cr
Plot type. The available options are:
\itemize{
\item \code{"ribbons"}: Time series with gradient-colored risk ribbon below.
Requires classified data from \code{\link[=classify_resilience]{classify_resilience()}}.
\item \code{"lines"}: Faceted line plots of raw metric values.
}}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object that can be further customized with
standard ggplot2 functions (e.g., \code{+ theme()}, \code{+ labs()}).
}
\description{
Visualizes resilience metric trajectories alongside the original time
series, either as a combined ribbon plot or as faceted line plots.
}
\details{
Two complementary visualizations are available:

\strong{\code{type = "ribbons"} (default).} Produces a single-panel plot combining
the original time series (black line, upper portion) with a set of
gradient-colored horizontal bands below it. Each band corresponds to one
resilience metric (plus the composite score). Color at each time point
encodes the directional score from \code{\link[=classify_resilience]{classify_resilience()}}, ranging from
green ("Excellent") through yellow ("Fair") to red ("Troubled"). This
layout answers the question \emph{"when and where does the system shift between
resilience states?"} at a glance, because all metrics share the same time
axis. Requires classified data; call \code{\link[=classify_resilience]{classify_resilience()}} first.

\strong{\code{type = "lines"}.} Produces a faceted plot with one panel per raw
metric, each showing the metric's rolling-window value over time. No
classification is needed. This view answers the question \emph{"what is the
raw trajectory of each metric?"} and is useful for diagnosing which
metrics drive changes visible in the ribbon plot. Panels use free y-axes
so that metrics on different scales are individually legible.
}
\examples{
\donttest{
set.seed(42)
x <- cumsum(rnorm(500))
res <- resilience(x, window = 50L)

# Faceted raw metric trajectories (no classification needed)
plot(res, type = "lines")

# Ribbon plot (requires classification first)
cls <- classify_resilience(res)
plot(cls, type = "ribbons")
}
}
\seealso{
\code{\link[=resilience]{resilience()}} for computing the metrics;
\code{\link[=classify_resilience]{classify_resilience()}} for directional normalization required by the
ribbon plot.

Other resilience: 
\code{\link{classify_resilience}()},
\code{\link{resilience}()}
}
\concept{resilience}
\concept{time series}
