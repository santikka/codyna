% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/potential.R
\name{potential_analysis}
\alias{potential_analysis}
\title{Stability Landscape and Potential Analysis}
\usage{
potential_analysis(
  data,
  window = NULL,
  n_bins = 50L,
  bandwidth = NULL,
  detrend = "none"
)
}
\arguments{
\item{data}{[\code{numeric} or \code{ts}]\cr
The time series to analyze. Accepts a numeric vector or a \code{ts} object.}

\item{window}{[\code{integer(1)}: \code{NULL}]\cr
Rolling window size. If \code{NULL} (default), a single global landscape
is estimated from the entire series. If specified, the landscape is
re-estimated for each window position.}

\item{n_bins}{[\code{integer(1)}: \code{50L}]\cr
Number of bins for the kernel density estimate grid. The actual grid
resolution passed to \code{stats::density()} is \code{n_bins * 4} to ensure
smooth interpolation.}

\item{bandwidth}{[\code{numeric(1)}: \code{NULL}]\cr
Bandwidth for the kernel density estimate. If \code{NULL}, Silverman's
rule of thumb is used (the \code{stats::density()} default).}

\item{detrend}{[\code{character(1)}: \code{"none"}]\cr
Detrending method applied before estimating the landscape. One of
\code{"none"}, \code{"linear"}, or \code{"diff"}.}
}
\value{
An object of class \code{"potential"} (a list) containing:
\itemize{
\item \code{landscape}: [\code{tibble}] Grid of the estimated landscape with
columns \code{x} (state-space position), \code{density} (probability
density), and \code{potential} (\eqn{-\log(\mathrm{density})}).
Present only for global analysis (\code{window = NULL}).
\item \code{wells}: [\code{tibble}] One row per detected well with columns
\code{location} (x position), \code{depth} (barrier height minus well
bottom), and \code{width} (distance between adjacent barriers).
Present only for global analysis.
\item \code{barriers}: [\code{tibble}] One row per detected barrier with
columns \code{location} (x position) and \code{height} (potential value).
Present only for global analysis.
\item \code{n_wells}: [\code{integer(1)}] Number of detected wells.
\item \code{values}: [\code{numeric()}] The (possibly detrended) data used
for the analysis.
\item \code{time}: [\code{numeric()}] The time index.
\item \code{rolling}: [\code{tibble}] Present only for rolling analysis
(\code{window} specified). Contains columns \code{time}, \code{value},
\code{n_wells}, and \code{dominant_well_location}.

Attributes: \code{n_bins}, \code{bandwidth}, \code{detrend}, \code{window}.
}
}
\description{
Estimates the quasi-potential landscape \eqn{U(x) = -\log P(x)} from
kernel density estimation, identifying stable attractors (potential
wells) and unstable equilibria (barriers). Supports global analysis of
the full series or rolling-window analysis to track how the number and
location of attractors change over time.
}
\details{
The potential function is derived from the Boltzmann relation between
probability density and energy: \eqn{P(x) \propto \exp(-U(x)/D)},
where \eqn{D} is a noise intensity parameter. Setting \eqn{D = 1}
gives \eqn{U(x) = -\log P(x)}. The probability density \eqn{P(x)} is
estimated via \code{stats::density()} with a Gaussian kernel.

\strong{Wells} (local minima of \eqn{U}) correspond to stable attractors --
states the system tends to occupy. \strong{Barriers} (local maxima of
\eqn{U}) correspond to unstable equilibria separating attractors.

Well depth measures how much "energy" is needed to escape an attractor:
deeper wells are more stable. Well width measures the basin of
attraction in state space. Together, depth and width characterise the
resilience of each attractor -- deep, wide wells indicate robust
stability, while shallow, narrow wells are easily escaped by
perturbations.

\strong{Detrending.} Non-stationary trends can bias the density estimate and
create spurious features. The \code{detrend} parameter offers three options:
\itemize{
\item \code{"none"} (default): No detrending. Appropriate for stationary
or nearly stationary data.
\item \code{"linear"}: Removes a linear trend via OLS before estimating
the landscape. Appropriate when the data has a slow drift.
\item \code{"diff"}: Uses first differences \eqn{\Delta x_t = x_t -
    x_{t-1}}. Appropriate for integrated processes (random walks).
Note: reduces series length by one.
}

\strong{Rolling-window mode.} When \code{window} is specified, the landscape is
re-estimated at each window position and the number of wells and the
location of the dominant (deepest) well are tracked over time. Changes
in the number of wells -- especially transitions from one to two wells
or vice versa -- can signal an approaching bifurcation (tipping point).
}
\examples{
\donttest{
# Single well: Ornstein-Uhlenbeck process (attractor at 0)
set.seed(42)
n <- 2000
x <- numeric(n)
for (i in 2:n) {
  x[i] <- x[i-1] - 0.5 * x[i-1] * 0.01 + 0.3 * rnorm(1)
}
pa <- potential_analysis(x)
pa
plot(pa)

# Two wells: bimodal mixture
set.seed(42)
bimodal <- c(rnorm(1000, -2, 0.5), rnorm(1000, 2, 0.5))
pa2 <- potential_analysis(bimodal)
summary(pa2)
plot(pa2, type = "both")
}
}
\references{
Livina, V.N., Kwasniok, F., & Lenton, T.M. (2010). Potential analysis
reveals changing number of climate states during the last 60 kyr.
\emph{Climate of the Past}, 6(1), 77--82.
\doi{10.5194/cp-6-77-2010}

Scheffer, M., Carpenter, S.R., Lenton, T.M., Bascompte, J.,
Brock, W., Dakos, V., van de Koppel, J., van de Leemput, I.A.,
Levin, S.A., van Nes, E.H., Pascual, M., & Vandermeer, J. (2012).
Anticipating critical transitions.
\emph{Science}, 338(6105), 344--348.
\doi{10.1126/science.1225244}

Strogatz, S.H. (2015). \emph{Nonlinear Dynamics and Chaos} (2nd ed.).
Westview Press.
}
\seealso{
\code{\link[=resilience]{resilience()}} for rolling resilience metrics;
\code{\link[=hurst]{hurst()}} for long-range dependence analysis;
\code{\link[=compute_trend]{compute_trend()}} for trend classification.
}
