% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.changepoint}
\alias{plot.changepoint}
\title{Plot Changepoint Detection Results}
\usage{
\method{plot}{changepoint}(x, type = "series", ...)
}
\arguments{
\item{x}{[\code{changepoint}]\cr
An object of class \code{changepoint} as returned by
\code{\link[=detect_cpts]{detect_cpts()}}.}

\item{type}{[\code{character(1)}: \code{"series"}]\cr
Plot type: \code{"series"} for colored segments with changepoint lines,
\code{"diagnostics"} for segment mean step function, or \code{"both"} for
both panels stacked.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object (or a \code{\link[patchwork:wrap_plots]{patchwork::wrap_plots()}}
composite when \code{type = "both"}).
}
\description{
Visualizes detected changepoints overlaid on the original time series.
Supports a series view with colored segments, a diagnostics view showing
segment means as a step function, or both panels stacked.
}
\details{
Three plot types are available:

\strong{\code{type = "series"} (default).} The original time series is drawn as a
line with segments colored by their segment ID. Vertical dashed lines
mark changepoint locations. This view immediately shows where the series
was split and how distinct the segments are.

\strong{\code{type = "diagnostics"}.} The segment means are drawn as a step
function (horizontal lines at each segment's mean), with vertical dashed
lines at changepoint locations and points marking the mean of each
segment. This view focuses on the magnitude and direction of each
detected shift.

\strong{\code{type = "both"}.} Stacks the series panel on top and the diagnostics
panel below using \code{\link[patchwork:wrap_plots]{patchwork::wrap_plots()}}, with shared time axes.
}
\examples{
\donttest{
set.seed(42)
x <- c(rnorm(200, 0, 1), rnorm(200, 5, 1))
cpts <- detect_cpts(x, method = "pelt")
plot(cpts)
plot(cpts, type = "diagnostics")
plot(cpts, type = "both")
}
}
\seealso{
\code{\link[=detect_cpts]{detect_cpts()}} for computing the changepoint analysis.
}
