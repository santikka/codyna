% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_tna.R
\name{to_tna}
\alias{to_tna}
\alias{to_tna.default}
\alias{to_tna.hurst_global}
\alias{to_tna.resilience}
\alias{to_tna.hurst}
\alias{to_tna.hurst_ews}
\alias{to_tna.multi_ews}
\alias{to_tna.trend}
\alias{to_tna.changepoint}
\alias{to_tna.spectral}
\alias{to_tna.potential}
\alias{to_tna.surrogate_test}
\alias{to_tna.sensitivity_ews}
\title{Extract State Sequences as Wide-Format Data}
\usage{
to_tna(x, ...)

\method{to_tna}{default}(x, ...)

\method{to_tna}{hurst_global}(x, ...)

\method{to_tna}{resilience}(x, state = "composite", ...)

\method{to_tna}{hurst}(x, state = "state", ...)

\method{to_tna}{hurst_ews}(x, state = "warning_label", ...)

\method{to_tna}{multi_ews}(x, ...)

\method{to_tna}{trend}(x, ...)

\method{to_tna}{changepoint}(x, state = "state", ...)

\method{to_tna}{spectral}(x, ...)

\method{to_tna}{potential}(x, ...)

\method{to_tna}{surrogate_test}(x, ...)

\method{to_tna}{sensitivity_ews}(x, ...)
}
\arguments{
\item{x}{An analysis object (resilience, hurst, hurst_ews, multi_ews, trend,
changepoint, spectral, or potential).}

\item{...}{Additional arguments passed to methods.}

\item{state}{Which column to extract as the TNA sequence. \code{"state"}
(default) extracts the combined \code{level_changepoint_type} label.
Also accepts \code{"level"}, \code{"direction"}, \code{"regime"}, or \code{"segment"}.}
}
\value{
A tibble with one row per sequence. Column names are \code{T1}, \code{T2},
etc. All values are character.
}
\description{
Extracts the categorical state trajectory from an analysis object and
returns it as a wide-format tibble with one row per sequence and columns
\verb{T1, T2, T3, ...} containing the state at each time point. The output
is directly compatible with \code{tna::prepare_data()} for transition network
analysis, \code{TraMineR::seqdef()} for sequence analysis, or any method
that expects wide-format categorical sequences.
}
\details{
Each analysis in the pipeline produces a categorical state ribbon over time:
\itemize{
\item \code{resilience}:  Six-level resilience categories (Excellent to Troubled)
from \code{classify_resilience()}. Available for the composite score and
each individual metric.
\item \code{hurst}: Five Hurst memory states (strong_antipersistent to
strong_persistent), or four multifractal categories when using MFDFA.
\item \code{hurst_ews}: Five warning levels (none to critical) from
\code{detect_hurst_warnings()}.
\item \code{multi_ews}: System states (Stable to Failing) from expanding-window
multivariate EWS. Rolling-window objects have no state classification
and are not supported.
\item \code{trend}: Four trend states (ascending, descending, flat, turbulent)
from \code{compute_trend()}.
\item \code{changepoint}: Regime state labels combining level and changepoint
type (e.g., \code{high_change}, \code{low_return}, \code{medium_initial}) from
\code{detect_cpts()}. The \code{state} argument selects which column
to extract: \code{"state"} (default), \code{"level"}, \code{"direction"},
\code{"regime"}, or \code{"segment"}.
\item \code{spectral}: Four noise-colour states (white_noise, pink_noise,
red_noise, brownian) from \code{spectral_ews()} with \code{states = TRUE}.
\item \code{potential}: Stability states (unimodal, bimodal, multimodal) derived
from the rolling well count in \code{potential_analysis()}. Requires
rolling-window mode (\code{window} specified).
}
}
\examples{
\donttest{
# Resilience state sequences
set.seed(42)
x <- cumsum(rnorm(500))
res <- resilience(x, window = 50L)
cls <- classify_resilience(res)
to_tna(cls)                    # composite categories
to_tna(cls, state = "vsi")     # per-metric categories

# Hurst memory state sequences
h <- hurst(x, method = "dfa", window = 50L, step = 10L)
to_tna(h)

# EWS warning level sequences
ews <- detect_hurst_warnings(h)
to_tna(ews)
to_tna(ews, state = "state")   # underlying Hurst states

# Changepoint segment sequences
cp <- detect_cpts(x, method = "pelt")
to_tna(cp)

# Spectral noise-colour sequences
sp <- spectral_ews(x, window = 50L, states = TRUE)
to_tna(sp)

# Potential stability state sequences (rolling mode)
pot <- potential_analysis(x, window = 100L)
to_tna(pot)
}
}
